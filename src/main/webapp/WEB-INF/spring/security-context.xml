<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

	<bean id="customAccessDenied" class="com.webnovel.security.CustomAccessDeniedHandler" />
	<bean id="customLoginSuccess" class="com.webnovel.security.CustomLoginSuccessHandler" >
		<property name="defaultUrl" value="/main/main"></property>
	</bean>
	<bean id="bcryptPasswordEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder" />
	
	<security:http>
		<security:intercept-url pattern="/" access="permitAll" />
		<security:intercept-url pattern="/member/mypage/*" access="hasRole('ROLE_MEMBER')" />
		<security:intercept-url pattern="/board/boardNewForm" access="hasRole('ROLE_MEMBER')" />
		<security:intercept-url pattern="/board/boardList" access="hasRole('ROLE_MEMBER')" />
		<security:intercept-url pattern="/admin/**" access="hasRole('ROLE_ADMIN')" />
		
		<security:access-denied-handler ref="customAccessDenied" />
		<!-- <security:csrf disabled="true"/> -->
		
		<security:form-login login-page="/member/loginForm" authentication-success-handler-ref="customLoginSuccess" />
		<!-- <security:form-login login-page="/member/loginForm" /> -->
		
		<security:logout logout-url="/member/logout" invalidate-session="true" />
	</security:http>

	<security:authentication-manager>
		<security:authentication-provider>
			<security:jdbc-user-service
				data-source-ref="dataSource" 
				users-by-username-query="select id, password, enabled from w_member where id = ? " 
				authorities-by-username-query="select id, auth from w_member_auth where id = ? " />
			<security:password-encoder ref="bcryptPasswordEncoder" />
		</security:authentication-provider>
	</security:authentication-manager>
</beans>
